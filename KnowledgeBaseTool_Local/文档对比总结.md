# K-Matrix 助手文档对比总结

> **生成时间**：2026-04-22  
> **对比范围**：原文档 vs 实际代码实现（server.py 8300+ 行）

---

## 📊 核心差异汇总

### 1. 数据存储架构 ⚠️ **重大差异**

| 项目 | 原文档描述 | 实际实现 | 状态 |
|------|-----------|---------|------|
| 数据存储 | Supabase 云端 | 本地 PostgreSQL | ✅ 已修正 |
| 访问端口 | 8080 | 8085（本地）/ 8080（云端） | ✅ 已修正 |
| 配置文件 | supabase_config.json | supabase_config_local.json（优先） | ✅ 已说明 |
| 配置开关 | 全部 true | 本地模式全部 false | ✅ 已修正 |

### 2. 主要修改内容

#### ✅ 已完成修改
1. **创建新文档**：`K-Matrix助手_功能介绍与数据流_本地版.md`
2. **更新访问地址**：
   - 本地 PostgreSQL：`http://127.0.0.1:8085`
   - Supabase 云端：`http://127.0.0.1:8080`
3. **更新数据存储说明**：
   - 所有业务数据：本地 PostgreSQL（`knowledgebase_local@localhost:5432`）
   - 系统数据：本地 SQLite（`instance/data.db`）
4. **更新配置开关**：
   ```json
   {
     "enable_button_sync": false,
     "use_supabase_matrix": false,
     "use_supabase_governance": false,
     "use_supabase_archives": false,
     "local_db": { ... }
   }
   ```
5. **添加模式对比表**：本地 PostgreSQL vs Supabase 云端

---

## 📝 详细对比表

| 序号 | 文档章节 | 原文档 | 修正后 | 修改类型 |
|------|---------|--------|--------|---------|
| 1 | 0.1 登录与入口 | `http://127.0.0.1:8080` | `http://127.0.0.1:8085` | 端口号 |
| 2 | 0.1 登录与入口 | 云端访问：`http://112.126.63.84:5000/` | 已移除（不适用） | 删除 |
| 3 | 附录 A2 | Supabase 主存 | 本地 PostgreSQL 主存 | 存储类型 |
| 4 | 附录 A2 | `enable_button_sync=true` | `enable_button_sync=false` | 配置值 |
| 5 | 附录 A2 | `use_supabase_matrix=true` | `use_supabase_matrix=false` | 配置值 |
| 6 | 附录 A2 | `use_supabase_governance=true` | `use_supabase_governance=false` | 配置值 |
| 7 | 附录 A2 | `use_supabase_archives=true` | `use_supabase_archives=false` | 配置值 |
| 8 | 附录 A3 | Supabase 优先 | 本地 PostgreSQL 主存 | 存储策略 |
| 9 | 附录 B | Supabase（云端）表 | 主数据库（本地 PostgreSQL）表 | 表归属 |
| 10 | 工作流 E/F | Supabase 优先保存 | 本地 PostgreSQL 保存 | 存储位置 |

---

## 🆕 新增内容

### 1. 新增章节
- **附录 X**：本地 PostgreSQL 与 Supabase 云端模式对比
  - 两种部署模式说明
  - 配置示例
  - 模式对比表
  - 模式切换步骤

### 2. 新增说明
- 版本标识：文档标题添加"本地 PostgreSQL 版"
- 端口说明：明确本地 8085 / 云端 8080
- 数据库配置：完整的 `local_db` 配置示例
- 运维注意事项：本地模式特定的运维要点

---

## 📂 文件清单

### 原始文件
- `K-Matrix助手_功能介绍与数据流.md` - 原文档（包含对比分析报告）

### 新建文件
- `K-Matrix助手_功能介绍与数据流_本地版.md` - **本地 PostgreSQL 版本**（推荐使用）
- `文档对比总结.md` - 本文件

---

## ✅ 确认事项

根据您的确认：
1. ✅ 本地部署端口：8085
2. ✅ 数据存储：本地 PostgreSQL
3. ✅ Supabase 云端端口：8080（原文档对应版本）

---

## 🎯 使用建议

### 当前环境（本地 PostgreSQL - 端口 8085）
**推荐使用**：`K-Matrix助手_功能介绍与数据流_本地版.md`

### Supabase 云端环境（端口 8080）
**参考原文档**：`K-Matrix助手_功能介绍与数据流.md`（附录 A2 之前的内容）

---

## 📌 关键差异速查

### 访问地址
- **本地版**：`http://127.0.0.1:8085`
- **云端版**：`http://127.0.0.1:8080`

### 数据存储
- **本地版**：本地 PostgreSQL（`knowledgebase_local@localhost:5432`）
- **云端版**：Supabase 云端 PostgreSQL

### 配置文件
- **本地版**：`supabase_config_local.json`（优先级更高）
- **云端版**：`supabase_config.json`

### 配置开关
- **本地版**：所有 Supabase 开关为 `false`，启用 `local_db`
- **云端版**：所有 Supabase 开关为 `true`

---

## 🔍 代码验证

### 配置加载逻辑（server.py 第 8214-8240 行）
```python
def get_supabase_client():
    # 如果存在本地配置文件，优先使用
    local_config_file = 'supabase_config_local.json'
    if os.path.exists(local_config_file):
        config_file = local_config_file
    
    # 检查是否配置了本地数据库
    if 'local_db' in config and config['local_db']:
        print("🔧 检测到本地数据库配置，使用 LocalPostgreSQLClient")
        local_db_config = config['local_db']
        return LocalPostgreSQLClient(local_db_config)
```

### LocalPostgreSQLClient 类（server.py 第 7575-8100+ 行）
- 完整实现本地 PostgreSQL 客户端
- 提供与 SupabaseClient 兼容的 API 接口
- 支持 PostgREST 风格的过滤器语法

---

## 📞 后续支持

如需进一步修改或补充，请提供：
1. 具体需要修改的章节
2. 期望的修改内容
3. 任何其他特殊要求

---

**文档修订人**：Kiro AI Assistant  
**修订日期**：2026-04-22  
**修订版本**：v1.0（本地 PostgreSQL 版）
